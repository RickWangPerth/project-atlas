# Templates

This directory is reserved for future deterministic output templates.

Planned template areas:

- Markdown data dictionary pages
- Mermaid diagram wrappers
- DBML output formatting helpers

Templates should stay simple and reviewable. They should consume normalized
metadata and optional knowledge overlays, not database connections.
