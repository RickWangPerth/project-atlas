# Proofs Of Concept

Use this directory for short-lived design experiments that help validate Atlas
interfaces before production implementation.

POCs should:

- describe the question being tested
- use small fixture data
- avoid secrets and production exports
- avoid becoming required production code without review

When a POC leads to a design decision, capture that decision in `docs/adr`.
